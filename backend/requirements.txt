# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary,pool]==3.2.3
psycopg_pool==3.2.4

# --- Cache / queue ---
redis==5.2.1
celery==5.4.0

# --- Auth & security ---
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# --- ML (wired in Phase 5-7) ---
scikit-learn==1.6.0
xgboost==2.1.4
lightgbm==4.5.0
imbalanced-learn==0.13.0
optuna==4.1.0
shap==0.46.0
matplotlib==3.10.8
pandas==2.2.3
numpy==2.2.1
joblib==1.4.2

# --- LLM integrations (wired in Phase 10) ---
# Ollama/Groq/OpenAI-compatible providers are implemented directly with
# httpx rather than the groq/openai SDKs -- see app/llm/*.py docstrings
# for why (consistent request handling across all three, and testable
# via httpx.MockTransport without fighting SDK-specific client config).
httpx==0.28.1

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0

# --- Dev tooling ---
ruff==0.8.4
mypy==1.14.0
